# ── 母舰 (Vercel) 环境变量 ──────────────────────────────
# 在 Vercel Dashboard → Settings → Environment Variables 中配置

# 管理员密钥（用于查看所有用户数据）
ADMIN_KEY=your-admin-key-here

# 可选：数据库连接（默认使用内存存储，重启丢失）
# DATABASE_URL=postgresql://user:pass@host/db
